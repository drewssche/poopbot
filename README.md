# 💩 PoopBot — Telegram-бот для ежедневного трекинга

PoopBot — это Telegram-бот для дружеских чатов, который **каждый день в одно и то же время** спрашивает участников, покакали ли они сегодня, собирает ответы и показывает статистику.

Проект задумывался как **локальный бот для друзей**, а не массовый продукт.

---

## ✨ Что умеет бот

### Ежедневный опрос

Каждый день в **15:00 по таймзоне чата** бот публикует сообщение:

> Кто сегодня какал?

С кнопками:

- 💩 Я
- ❌ Не
- ⏳ Спроси меня позже (через 2 часа)

В сообщении:

- отображается список участников чата
- напротив каждого — статус (❓ / 💩 / ❌ / ⏳)
- рядом показывается **текущий стрик** (серия дней подряд с 💩)

---

### Напоминания

Если участник нажал **«Спроси меня позже»**:

- бот запоминает это
- через 2 часа (но не позже конца дня) присылает напоминание в общий чат
- напоминание тегает **только этого пользователя**
- напоминание можно получить **не чаще одного раза за сессию**

---

### Второй вопрос

Если **хотя бы один участник** ответил 💩, бот публикует второй вопрос:

> Как прошёл процесс?

С кнопками:

- 😇 Прекрасно
- 😐 Сойдёт
- 😫 Ужасно

Ответить на него могут **только те**, кто выбрал 💩 в первом вопросе.

---

### Статистика `/stats`

Команда `/stats` (и кнопка 📊 в меню) показывает статистику **только для текущего чата**:

- Сегодня
- На этой неделе
- В этом месяце
- В этом году

Формат:

- **Итог по чату** (💩 / ❌, 😇 / 😐 / 😫, сколько участников ответили)
- **Строки по каждому участнику**
- Учитываются только ответы за выбранный период
- Есть кнопка **⬅️ Назад**

---

### Меню `/help`

Команда `/help` открывает меню с кнопками:

- ⚙️ Настройки
- 📊 Статистика
- ℹ️ О боте

В настройках можно:

- 🚫 Не участвовать (не тегаться в опросах)
- 🧹 Стереть мои данные (удаление из базы для этого чата)

---

## 🧠 Логика и ограничения

- Один пользователь может ответить **только один раз в день**
- Сессия длится с 00:00 до 23:55 (по таймзоне чата)
- После закрытия сессии кнопки перестают работать
- Стрик увеличивается **только при 💩**
- Стрик `0` показывается без даты (чтобы не выглядело багом)

---

## 🛠️ Технологии

- Python 3.12
- aiogram 3
- PostgreSQL
- Docker + Docker Compose
- APScheduler

---

## 🚀 Запуск локально

### 1. Клонировать репозиторий

```bash
git clone https://github.com/USERNAME/poopbot.git
cd poopbot
```

### 2. Создать `.env`

Создай файл `.env` в корне проекта и заполни его так:

```env
BOT_TOKEN=your_telegram_bot_token
DB_HOST=postgres
DB_PORT=5432
DB_NAME=poopbot
DB_USER=poopbot
DB_PASSWORD=poopbot
CHAT_TIMEZONE=Europe/Minsk
```

> ⚠️ Файл `.env` **не должен попадать в GitHub**.  
> Убедись, что он добавлен в `.gitignore`.

### 3. Запустить проект

```bash
docker compose up -d --build
```

### 4. Проверить, что всё работает

```bash
docker ps
docker logs poopbot-app --tail 100
```

Если бот запущен корректно — ошибок в логах быть не должно.

---

## 🤖 Подключение бота к чату

1. Добавь бота в Telegram-группу
2. Назначь бота **администратором**
3. Дай права:
   - редактировать сообщения
   - удалять сообщения (по желанию)
4. В чате напиши команду:

```text
/start
```

После этого бот создаст ежедневный опрос.

---

## 🧹 Очистка данных для тестирования

Если нужно сбросить дневные данные и начать тест заново:

```sql
TRUNCATE TABLE
  reminders,
  q2_answers,
  q1_answers,
  daily_sessions
RESTART IDENTITY
CASCADE;
```

⚠️ Таблицы `participants` и `chats` при этом **не трогаются**.

---

## 📦 Статус проекта

- MVP завершён
- Подходит для небольших дружеских чатов
- Работает через Docker Compose
- Готов к деплою на VPS/VDS
